spec:
  template:
    spec:
      containers:
      - name: frontend-app
        command:
        - /bin/sh
        - -c
        - |
          sed -i "s/Education Platform/Horizons TSA/g" /app/src/app/auth/login/login.component.html
          npm start
